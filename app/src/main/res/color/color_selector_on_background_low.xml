<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
  <item
    android:state_pressed="true"
    android:color="@color/color_onBackground_low1"/>

  <!-- 选中状态 -->
  <item
    android:state_selected="true"
    android:color="@color/color_onBackground"/>

  <!-- 未选中（默认） -->
  <item android:color="@color/color_onBackground_low"/>
</selector>
